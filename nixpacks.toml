[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm ci --ignore-scripts"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "node backend/dist/backend/src/db/migrate.js && node backend/dist/backend/src/server.js"
